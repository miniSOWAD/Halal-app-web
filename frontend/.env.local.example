# Local backend
# NEXT_PUBLIC_API_URL=http://localhost:8000/api

# FastAPI Cloud backend. The frontend also corrects a missing /api suffix,
# but including it here keeps the configuration explicit.
NEXT_PUBLIC_API_URL=https://backend.fastapicloud.dev/api
