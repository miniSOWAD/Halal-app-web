import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.halalfit.global",
  appName: "HalalFit",
  webDir: "out",
  server: { androidScheme: "https" },
};

export default config;
